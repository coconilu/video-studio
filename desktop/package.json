{
  "name": "video-studio-desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Video Studio 桌面壳：内嵌 Node sidecar 跑平台 server，webview 加载 127.0.0.1",
  "scripts": {
    "icon": "node tools/make-icon.mjs && tauri icon src-tauri/icons/app-icon.png",
    "bundle": "node scripts/bundle-platform.mjs && node scripts/bundle-runtime.mjs",
    "dev": "tauri dev",
    "build": "npm run bundle && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
